make more scrap script, modify existing
To clude more field into table
modify the UI
optional: switch to sql db
optional: deploy on cloud
optional: table could be ordered by any field
optional: system could ordered be by overall performance score